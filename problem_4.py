# Problem 4
#Allow a user to enter two floating point numerical values. Display the product of the two numbers. 

v1 = float(input("Enter a floating point: "))
v2 = float(input("Enter another floating point: "))

product = v1 * v2
print(product)